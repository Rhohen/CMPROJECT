programme premiertest:

const min=10; max=25; oui = vrai; nope = faux;
var ent i;
	bool b1, b2, b3;

debut
	lire(i);
	lire(b1);
	ecrire(i);
	ecrire(b1);
	i:= (max-min) div 2;
	b1:= oui et nope;
	b2:= nope ou (nope ou non nope);
	b3:= (i=7);
	ecrire(i);
	ecrire(b1,b2,b3);
fin